name: Keep the staging API warm

# Free hosting tiers put an idle instance to sleep, and waking it takes long
# enough that the first request after a quiet hour can exceed the mobile client's
# 30-second timeout. The parent then sees "İnternet bağlantın yok gibi" — honest
# copy for the wrong reason, since their connection is fine and our server was
# asleep.
#
# A request every ten minutes costs seconds of Actions time and removes the
# problem entirely. It does nothing unless the STAGING_API_URL repository
# variable is set, so a fork or a repo without a deploy stays silent rather than
# failing a scheduled job forever.
#
# Two things worth knowing: GitHub disables scheduled workflows on a repository
# with no activity for 60 days, and this keeps a free instance's monthly hours
# ticking. If the free allowance runs short, lengthen the interval rather than
# deleting this — a sleeping instance is a worse first impression than a slow one.

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: keep-warm
  cancel-in-progress: true

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping readiness
        env:
          API_URL: ${{ vars.STAGING_API_URL }}
        run: |
          if [ -z "$API_URL" ]; then
            echo "STAGING_API_URL is not set; nothing to keep warm."
            exit 0
          fi

          # Generous timeout on purpose: a cold start is exactly what this is
          # here to absorb, so a slow answer is success, not failure.
          code=$(curl -sS -o /tmp/ready.json -w '%{http_code}' --max-time 90 \
            "${API_URL%/}/health/ready" || echo 000)

          echo "HTTP $code"
          cat /tmp/ready.json 2>/dev/null || true
          echo

          # A failure here means the deploy is down, which is worth seeing in the
          # Actions tab rather than swallowing.
          [ "$code" = "200" ]
