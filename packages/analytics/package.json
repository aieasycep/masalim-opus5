{
  "name": "@masalim/analytics",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@masalim/types": "workspace:*"
  },
  "devDependencies": {
    "@masalim/config": "workspace:*",
    "eslint": "^9.18.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
